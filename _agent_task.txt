You are the Engineering Team Lead for Skissify. Do the following in order:

TASK: Add Ink Color Picker to Sidebar + Fix Blueprint Board Background

PART 1: Ink Color Picker in Sidebar
The sketch has an inkColor field (e.g. #111) but there is NO UI for it - users can only change
it via the raw JSON editor. Add a proper color picker to the ControlPanel sidebar.

Steps:
1. In src/hooks/useSketch.ts, add a setInkColor callback (model on setPaper):
   const setInkColor = useCallback((inkColor: string) => updateSketch({ inkColor }), [updateSketch]);
   Export it from the return statement.

2. In src/components/ControlPanel.tsx:
   - Add inkColor: string and onInkColor: (c: string) => void to the ControlPanelProps interface
   - Add a new "Ink Color" section after the Tool section
   - Preset colors array: { key: "#111", label: "Black" }, { key: "#1a3a8c", label: "Blue" }, 
     { key: "#8b1a1a", label: "Red" }, { key: "#1a4a20", label: "Green" }, 
     { key: "#8b6900", label: "Brown" }, { key: "#555", label: "Gray" },
     { key: "#1a5a5a", label: "Teal" }, { key: "#663399", label: "Purple" }
   - Render as a grid of colored square buttons (w-6 h-6), active one gets ring-2 ring-blue-500
   - Below the grid, add a row with an <input type="color"> for custom color picking,
     styled as a small swatch button next to a "Custom" label
   - When the color input changes, call onInkColor(e.target.value)

3. In src/app/editor/page.tsx:
   - Destructure setInkColor from the useSketch() call
   - Pass inkColor={sketch.inkColor} and onInkColor={setInkColor} to <ControlPanel>

PART 2: Fix Blueprint Board Background Visual
In src/lib/renderer.ts, in the drawPaperBackground function, change the blueprint case to:
  // Fill entire canvas with dark board color first
  ctx.fillStyle = "#0d1f3a";
  ctx.fillRect(0, 0, w, h);
  // Then draw the paper (blueprint) with 4px inset
  const boardMargin = 4;
  const grad = ctx.createLinearGradient(boardMargin, boardMargin, boardMargin, h - boardMargin);
  grad.addColorStop(0, "#1a3a6b");
  grad.addColorStop(0.5, "#1e4278");
  grad.addColorStop(1, "#16305e");
  ctx.fillStyle = grad;
  ctx.fillRect(boardMargin, boardMargin, w - boardMargin * 2, h - boardMargin * 2);

Also in the drawGrid and drawPaperTexture functions for blueprint mode, offset by the same 4px
boardMargin so the grid/texture don't overlap the dark border. Add const boardMargin = 4; 
at the top of each and adjust: replace ctx.fillRect(0, 0, w, h) and loop limits from 0 to boardMargin,
w/h to w-boardMargin / h-boardMargin respectively. This is optional/best-effort.

PART 3: Verify and Commit
1. Run: npm run build
2. If TypeScript errors exist, fix all of them before committing
3. git add -A
4. git commit -m "feat(editor): ink color picker in sidebar + blueprint board inset fix"
5. git push
6. Print a short summary of what you did

When completely done, run: openclaw system event --text "Done: Skissify ink color picker + blueprint board fix committed and pushed" --mode now
