# Handover: Skissify Docker & Auth Production Config

**Session:** avesta (2026-03-27)
**From:** skissify agent (W-/code/skissify)
**To:** skissify root agent

## What Changed

### Modified Files
1. **`Dockerfile`** — Added `/app/data` directory creation (line 37) for persistent SQLite DB storage
2. **`docker-compose.yml`** — Added `env_file: .env.production`, persistent volume `skissify-data:/app/data`, kept traefik-network
3. **`.gitignore`** — Added `.env.production` and `.webhook-secret` to prevent committing secrets

### New Files
4. **`.env.production.example`** — Production env var template with all required vars:
   - `DATABASE_URL=file:/app/data/skissify.db` (persistent volume)
   - `NEXTAUTH_URL=https://skissify.conzeon.dev`
   - Google OAuth, GitHub OAuth, Stripe, NEXTAUTH_SECRET placeholders

### Files Fixed (from proxmox agent)
5. **`.github/workflows/deploy.yml`** — Replaced old Proxmox webhook with SSH-based Hetzner deploy (was still pointing to deploy.conzeon.dev)
6. **`scripts/deploy.sh`** — Fixed path to `/root/skissify` and branch to `master`

### Files Removed
7. **`deploy.sh`** (root) — Redundant with `scripts/deploy.sh`
8. **`webhook-deploy.sh`** — Redundant with GitHub Actions SSH deploy

## Docker Build Verified
- Clean Docker build succeeds (66s)
- Container starts and serves HTTP 200
- Port mapping: 9700 (host) → 3000 (container)

## Key Architecture Decisions
- SQLite DB persisted in Docker volume at `/app/data/skissify.db`
- No in-app webhook — GitHub Actions SSH handles deploys
- NextAuth v5 handles auth (Google + GitHub OAuth) — no Traefik SSO

## Pending (User Action Required)
1. Google OAuth Client ID + Secret
2. NEXTAUTH_SECRET (auto-generated by hetzner-setup.sh)
3. HETZNER_SSH_KEY GitHub secret
4. First deploy on Hetzner server

## Important Note
Traefik must route to container port 3000 (not 9700). dnsify's config may need fixing — they used port 9700.
